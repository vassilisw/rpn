Requirements:
c++11

Use one of the following to compile:
  $ make # system default compiler
  $ make CXX=g++
  $ make CXX=clang++

and/or to install:
  $ sudo make install

for FreeBSD use `gmake` instead
